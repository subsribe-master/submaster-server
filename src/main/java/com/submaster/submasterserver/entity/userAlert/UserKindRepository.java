package com.submaster.submasterserver.entity.userAlert;

import org.springframework.data.jpa.repository.JpaRepository;

public interface UserKindRepository extends JpaRepository<UserAlert, Integer> {
}
